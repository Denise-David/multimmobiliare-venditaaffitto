import React from 'react';
import TextField from '@material-ui/core/TextField';

const TextName = () => (
  <TextField fullWidth style={{ marginLeft: 8 }} label="Cognome" />
);

export default TextName;
